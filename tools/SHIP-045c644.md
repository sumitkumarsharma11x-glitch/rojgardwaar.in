# SHIP PACKAGE — Release Science Preparation v1 (commit 045c644)

Status: audit COMPLETE (44/44), commit ready, push BLOCKED in the authoring session
(GitHub TLS unreachable from that sandbox). This file + tools/release-audit.mjs persist
in the workspace so the next session can ship immediately.

## Facts verified locally
- Commit `045c644` on branch `arena/01a06bbb-rojgardwaar-in` (92 files, +130,992).
- Committed bank totals: Biology 1,800 · Physics 2,050 · Chemistry 400 = **4,250**.
- Frozen checksums match: C01 f03ff6a09cb9e009 · C02 61e6a35f2f8bd24b ·
  P09 1625eb1169d950a2 · P10 e53361af5f65ad4c · P11 522c6a0f7a2f5cfb · P12 fdf2bb4a697e7491.
- Real merge-base on server = `250b6b9` (dadbd69's parent-1). The local
  "unrelated histories" error is a shallow-clone artifact only (.git/shallow
  lists dadbd69). Server-side merge is NORMAL.
- Conflict set (both sides changed since 250b6b9) = EXACTLY 24 paths:
  chapter.html, test.html, mock-test.html, css/mock-test.css, data/manifest.json,
  data/science/biology/chapter-01/{meta,questions}.json,
  data/science/biology/chapter-02/{meta,questions}.json, js/chapter-app.js,
  js/dataLoader.js, js/home.js, js/languageManager.js, js/mockTestApp.js,
  js/practiceEngine.js, js/randomizer.js, js/resultRenderer.js, js/scoreEngine.js,
  js/storageManager.js, js/testGenerator.js, js/testSession.js, js/timer.js,
  js/uiManager.js, js/wrongQuestionManager.js
  → resolve ALL as "ours" (045c644 verified versions).
- Main-only changes to PRESERVE: `index.html`, `sitemap.xml` (PR#1 versions;
  our branch never touched them since 250b6b9 → merge takes main's automatically,
  no conflict). Zero other main-only files (verified: diff-filter=A empty).
- Expected merged tree = 045c644 content + main's index.html/sitemap.xml.

## Ship recipe (next session)
```bash
git fetch origin
git checkout arena/01a06bbb-rojgardwaar-in
git log --oneline -1        # MUST show 045c644
git merge origin/main       # conflicts on the 24 paths listed above
git checkout --ours -- chapter.html test.html mock-test.html css/mock-test.css \
  data/manifest.json \
  data/science/biology/chapter-01/meta.json data/science/biology/chapter-01/questions.json \
  data/science/biology/chapter-02/meta.json data/science/biology/chapter-02/questions.json \
  js/chapter-app.js js/dataLoader.js js/home.js js/languageManager.js js/mockTestApp.js \
  js/practiceEngine.js js/randomizer.js js/resultRenderer.js js/scoreEngine.js \
  js/storageManager.js js/testGenerator.js js/testSession.js js/timer.js js/uiManager.js \
  js/wrongQuestionManager.js
git add -A
# sanity BEFORE committing the merge:
#   ! git diff --name-only --diff-filter=U          (no unresolved left)
#   bank totals still 4,250 (see checker below)
git commit -m "Merge origin/main; keep verified release versions (Science Preparation v1)"
git push origin arena/01a06bbb-rojgardwaar-in      # normal push, NEVER force
gh pr create --base main --head arena/01a06bbb-rojgardwaar-in \
  --title "Release Science Preparation v1 — 4,250 verified questions" \
  --body "26 chapters: B01-B12 (1,800) + P01-P12 (2,050) + C01-C02 (400). Audit 44/44. Conflict resolution: release versions for the 24 evolved app/data paths; main's index.html + sitemap.xml preserved."
gh pr merge --merge        # after CI/checks green
```

## Post-deploy verification
- Run `tools/release-audit.mjs` against local server (rebuild /home/user/validate:
  `npm i playwright@1.49.1 @sparticuz/chromium@149.0.0`; extract chromium +
  al2023 libs as in-script paths imply; serve repo on :8000). Point BASE at
  https://rojgardwaar.in for the live check.
- Live checks: / (eligibility), /mock-test.html (landing), chapter.html deep links
  (biology/physics/chem C01+C02), practice Q1/N + explanation, mock 30q + submit,
  revision 1/n, hi<->en, 0 JS errors, and totals 4,250 via manifest+banks.

## Bank totals checker (post-merge sanity)
```bash
python3 - <<'PY'
import json, subprocess
t = {}
for s in ("biology", "physics", "chemistry"):
    n = 0
    for c in range(1, 13):
        raw = subprocess.run(["git", "show", f"HEAD:data/science/{s}/chapter-{c:02d}/questions.json"], capture_output=True, text=True).stdout
        if not raw: continue
        d = json.loads(raw); n += len(d["questions"] if isinstance(d, dict) else d)
    t[s] = n
print(t, "TOTAL =", sum(t.values()), "(expect 4250)")
PY
```

## Do NOT
- Force push, rewrite history, reset/delete, modify C03, regenerate banks,
  touch index.html/sitemap.xml beyond taking main's versions automatically.
